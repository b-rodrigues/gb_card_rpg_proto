{
  "name": "font_test",
  "test": true,
  "description": "Render full ASCII charset and verify active GBDK font rendering. Screen assertions verified by dialogue_render_test.",
  "scenario_id": "FONT_TEST",
  "actions": [
    {
      "type": "wait",
      "frames": 10
    }
  ],
  "assertions": [
    {
      "type": "game_state",
      "expected": "OVERWORLD"
    }
  ]
}
